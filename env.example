# Environment file for imshare
# For systemd service: /etc/imshare/env
# For manual use: source this file or export directly

# REQUIRED: Secret key for JWT signing (use a strong random value)
# Generate with: openssl rand -base64 32
IMSHARE_SECRET=your-secret-key-here-change-me
